---
intent: Replace single balance label with four portfolio-value fields, add equity curve
        chart and open options panel, fix halt threshold to $0 instead of $100.
success_criteria: Four portfolio labels live; equity curve plots per-tick; options table
  renders positions; SafetyStop halts at $0; win rate % visible; all tests pass; dashboard opens.
risk_level: low
auto_approve: true
worktree: false
---

## Steps

- [x] **Step 1: Update SafetyStop threshold from $100 to $0**
action: In `trading-account/src/main/java/com/tradingapp/account/SafetyStop.java`, change `MINIMUM_BALANCE` from `100.0` to `0.0` and change the comparison from `account.getBalance() < MINIMUM_BALANCE` to `account.getBalance() <= MINIMUM_BALANCE`. Update the console message to read "SAFETY STOP: account balance $%.2f is zero or negative. All trading halted.". In `trading-account/src/test/java/com/tradingapp/account/AccountTest.java`, find all test assertions that relied on the $100 threshold (any test that sets balance to a value like 99.0 or 50.0 and expects trading to halt) and update them to use $0 or negative values as the trigger. Add a new test `safetyStopHaltsAtZeroBalance` that sets balance to 0.0 and verifies `safetyStop.check()` returns true. Add `safetyStopDoesNotHaltAtSmallPositiveBalance` that sets balance to 0.01 and verifies trading is not halted.
loop: false
verify: /Users/ericshort/.sdkman/candidates/maven/current/bin/mvn -pl trading-account test -f /Users/ericshort/AIProjects/TradingApp/pom.xml

- [x] **Step 2: Add OptionsPositionRow POJO in trading-ui**
action: Create `trading-ui/src/main/java/com/tradingapp/ui/OptionsPositionRow.java` with eight fields and matching getters: `String symbol`, `String type` (CALL or PUT), `String strike` (formatted as "$%.2f"), `String expiry` (LocalDate.toString()), `int contracts`, `String entryPremium` (formatted as "$%.4f"), `String currentPremium` (formatted as "$%.4f"), `String unrealizedPnl` (formatted with sign, e.g. "+$12.34" or "-$5.00"). Add a single all-args constructor that takes the raw doubles and formats all String fields internally. The `unrealizedPnl` field is computed as `(currentPremium - entryPremium) * 100 * contracts`.
loop: false
verify:
  type: artifact
  path: trading-ui/src/main/java/com/tradingapp/ui
  assert:
    kind: matches-glob
    value: "OptionsPositionRow.java"

- [x] **Step 3: Replace dashboard.fxml with new 3-section VBox layout**
action: Overwrite `trading-ui/src/main/resources/com/tradingapp/ui/dashboard.fxml` with a new layout. Root element: `VBox` with `style="-fx-background-color: #1a1a2e;"`. Add imports for `javafx.scene.chart.*` and `javafx.scene.chart.NumberAxis`. Section 1 — portfolio labels VBox (`style="-fx-background-color: #16213e; -fx-padding: 10;"`, `spacing="8"`): Row 1 is an HBox (`spacing="20"`) with labels `fx:id="totalPortfolioLabel"` (text="Total Portfolio: $100,000.00", green bold 18px), `fx:id="stockHoldingsLabel"` (text="Stocks: $0.00", white 14px), `fx:id="optionHoldingsLabel"` (text="Options: $0.00", white 14px), `fx:id="availableCashLabel"` (text="Cash: $100,000.00", white 14px), `fx:id="haltedLabel"` (text="", red bold 14px). Row 2 is an HBox (`spacing="20"`) with labels `fx:id="winsLabel"` (text="Wins: 0", green 14px), `fx:id="lossesLabel"` (text="Losses: 0", red 14px), `fx:id="winRateLabel"` (text="Win Rate: 0%", white 14px), `fx:id="pnlLabel"` (text="P&L: $0.00", white 14px). Section 2 — middle HBox (`VBox.vgrow="ALWAYS"`, `spacing="5"`, `style="-fx-padding: 5;"`, `prefHeight="280"`): Left side is a VBox (`HBox.hgrow="ALWAYS"`) with a Label ("Equity Curve", white bold 12px) and a `LineChart fx:id="equityChart"` (`HBox.hgrow="ALWAYS"` `VBox.vgrow="ALWAYS"` `animated="false"` `createSymbols="false"` `legendVisible="false"` `style="-fx-background-color: #1a1a2e;"`) containing `xAxis` as `NumberAxis` (`tickLabelsVisible="false"` `tickMarkVisible="false"` `minorTickVisible="false"` `forceZeroInRange="false"`) and `yAxis` as `NumberAxis` (`label="Portfolio ($)"` `forceZeroInRange="false"`). Right side is a VBox (`prefWidth="480"`) with a Label ("Open Options Positions", white bold 12px) and a `TableView fx:id="optionsTable"` (`VBox.vgrow="ALWAYS"` `style="-fx-background-color: #1a1a2e;"`) with columns: `fx:id="optColSymbol"` text="Symbol" prefWidth="60", `fx:id="optColType"` text="Type" prefWidth="50", `fx:id="optColStrike"` text="Strike" prefWidth="75", `fx:id="optColExpiry"` text="Expiry" prefWidth="95", `fx:id="optColContracts"` text="Qty" prefWidth="40", `fx:id="optColEntryPrem"` text="Entry Prem" prefWidth="90", `fx:id="optColCurrentPrem"` text="Curr Prem" prefWidth="90", `fx:id="optColUnrealizedPnl"` text="Unrealized P&L" prefWidth="105". Section 3 — bottom HBox (`VBox.vgrow="ALWAYS"` `spacing="5"` `style="-fx-padding: 5;"`): Left VBox (`HBox.hgrow="ALWAYS"` `spacing="3"`) with Label "Research" (white bold 12px), Separator, TextArea `fx:id="researchArea"` (`editable="false"` `wrapText="true"` `VBox.vgrow="ALWAYS"` `style="-fx-control-inner-background: #1a1a2e; -fx-text-fill: #c0c0c0; -fx-font-size: 11px;"`). Right VBox (`HBox.hgrow="ALWAYS"` `spacing="3"`) with Label "Trade History" (white bold 12px), Separator, TableView `fx:id="tradeHistoryTable"` (`VBox.vgrow="ALWAYS"` `style="-fx-background-color: #1a1a2e;"`) with the same eight columns as before (`colTimestamp`, `colSymbol`, `colAction`, `colQuantity`, `colPrice`, `colFee`, `colBalance`, `colReason` with same prefWidths as Phase 4).
loop: false
verify: /Users/ericshort/.sdkman/candidates/maven/current/bin/mvn -pl trading-ui compile -f /Users/ericshort/AIProjects/TradingApp/pom.xml

- [x] **Step 4: Update DashboardController with new fields, portfolio helpers, and refreshUi overhaul**
action: In `DashboardController.java`, promote `PriceHistory priceHistory` and `BlackScholesEngine bsEngine` from local variables in `initialize()` to private instance fields. Add `@FXML` fields: `Label totalPortfolioLabel`, `Label stockHoldingsLabel`, `Label optionHoldingsLabel`, `Label availableCashLabel`, `Label winRateLabel`, `LineChart<Number,Number> equityChart`, `TableView<OptionsPositionRow> optionsTable`, `TableColumn<OptionsPositionRow,String> optColSymbol`, `optColType`, `optColStrike`, `optColExpiry`, `optColContracts` (as `TableColumn<OptionsPositionRow,Integer>`), `optColEntryPrem`, `optColCurrentPrem`, `optColUnrealizedPnl` (all String columns except contracts). Remove the `@FXML Label balanceLabel` field (it no longer exists in FXML) and replace all `balanceLabel.setText(...)` calls with the equivalent `availableCashLabel.setText(...)`. Add instance fields `private XYChart.Series<Number,Number> equitySeries` and `private int tickCount = 0`. In `initialize()`, after constructing bsEngine and priceHistory: initialize the equitySeries (`equitySeries = new XYChart.Series<>(); equityChart.getData().add(equitySeries);`), call `setupOptionsTableColumns()`. Add private method `setupOptionsTableColumns()` that sets PropertyValueFactory for all eight options table columns: "symbol", "type", "strike", "expiry", "contracts", "entryPremium", "currentPremium", "unrealizedPnl". Add private method `double computeStockHoldings()`: iterate `account.getPositions().values()`, for each position get `priceHistory.getPrices(pos.getSymbol())`, use last element if non-empty else 0.0, return sum of `quantity * price`. Add private method `double computeOptionCurrentPremium(OptionsPosition pos)`: get prices list for pos.getSymbol(), if empty return 0.0; get last price; compute T via `bsEngine.timeToExpiry(pos.getExpiry())`; if T <= 0 return 0.0; compute vol via `bsEngine.historicalVol(prices)`; if vol == 0 return 0.0; return `bsEngine.callPrice(...)` or `bsEngine.putPrice(...)` depending on pos.getType(). Add private method `double computeOptionHoldings()`: iterate `account.getOptionsPositions().values()`, for each compute `pos.getCurrentValue(computeOptionCurrentPremium(pos))`, return sum. Overhaul `refreshUi()`: compute `availableCash = account.getBalance()`, `stockHoldings = computeStockHoldings()`, `optionHoldings = computeOptionHoldings()`, `totalPortfolio = availableCash + stockHoldings + optionHoldings`; update all four portfolio labels with `$%,.2f` format; compute `wins = transactionLog.countWins()`, `losses = transactionLog.countLosses()`, `total = wins + losses`, `winRate = total > 0 ? (wins * 100.0 / total) : 0.0`; update winsLabel, lossesLabel, pnlLabel, winRateLabel ("Win Rate: %.1f%%"); add equity chart point (`equitySeries.getData().add(new XYChart.Data<>(tickCount++, totalPortfolio))`); if `equitySeries.getData().size() > 200` remove index 0; rebuild options table rows from `account.getOptionsPositions().values()` into a list of `OptionsPositionRow` objects using `computeOptionCurrentPremium`; set `optionsTable.setItems(FXCollections.observableArrayList(rows))`; update halted label text to "⛔ TRADING HALTED — portfolio exhausted" when `account.isTradingHalted()`. Update `refreshBalance()` similarly for the four portfolio labels. Add required imports: `com.tradingapp.ui.OptionsPositionRow`, `javafx.scene.chart.LineChart`, `javafx.scene.chart.XYChart`, `com.tradingapp.account.OptionsPosition`.
loop: false
verify: /Users/ericshort/.sdkman/candidates/maven/current/bin/mvn -pl trading-account,trading-data,trading-engine,trading-ai,trading-options,trading-ui compile -f /Users/ericshort/AIProjects/TradingApp/pom.xml

- [x] **Step 5: Full test suite**
action: Run the complete Maven test suite to confirm all existing and new tests pass with zero failures.
loop: until all tests pass
max_iterations: 3
verify: /Users/ericshort/.sdkman/candidates/maven/current/bin/mvn test -f /Users/ericshort/AIProjects/TradingApp/pom.xml

- [x] **Step 6: Smoke test — dashboard launch**
action: Run `mvn install -DskipTests -f /Users/ericshort/AIProjects/TradingApp/pom.xml -q` to install all modules, then launch the dashboard with `mvn javafx:run -f /Users/ericshort/AIProjects/TradingApp/trading-ui/pom.xml`. Confirm the window opens with the new layout.
loop: false
verify:
  type: human-review
  prompt: Dashboard opens with four portfolio value labels (Total Portfolio, Stocks, Options, Cash), equity curve chart visible in top-left, open options positions table in top-right, research pane and trade history in the bottom half. No startup errors in console.
gate: human
